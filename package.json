{
  "name": "wager-marketplace",
  "private": true,
  "dependencies": {
    "@fastify/cors": "^10.0.2",
    "@fastify/jwt": "^9.0.2",
    "@prisma/client": "^6.4.1",
    "@vercel/node": "^5.1.8",
    "bcrypt": "^5.1.1",
    "fastify": "^5.2.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "prisma": "^6.4.1"
  },
  "scripts": {
    "dev": "pnpm --filter @wager/server dev",
    "build": "pnpm --filter @wager/server build",
    "start": "pnpm --filter @wager/server start",
    "test": "pnpm --filter @wager/server test",
    "db:migrate": "pnpm --filter @wager/server db:migrate",
    "db:seed": "pnpm --filter @wager/server db:seed",
    "db:generate": "pnpm --filter @wager/server db:generate"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "bcrypt",
      "esbuild",
      "prisma"
    ]
  }
}
